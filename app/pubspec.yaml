name: mewmer
description: "Create. Caption. Chaos."
publish_to: "none"

# Read more about Android versioning at: https://developer.android.com/studio/publish/versioning
# Read more about iOS versioning at: https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 0.0.1+1

environment:
    sdk: ^3.11.0

dependencies:
    flutter:
        sdk: flutter

    cupertino_icons: ^1.0.8 # Use with the CupertinoIcons class for iOS style icons.
    image_picker: ^1.2.2
    image: ^4.8.0
    path_provider: ^2.1.5
    screenshot: ^3.0.0
    google_fonts: ^8.1.0
    provider: ^6.1.5+1
    flutter_image_compress: ^2.4.0
    whatsapp_stickers_injector: ^1.1.3

dev_dependencies:
    flutter_test:
        sdk: flutter

    flutter_lints: ^6.0.0

flutter:
    uses-material-design: true
